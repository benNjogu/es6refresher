console.log([1, 2, 3, 2, 1].slice(2, 4)); //->[ 3, 2 ]

console.log([1, 2, 3, 2, 1].slice(2)); //->[ 3, 2, 1 ]
/**
 * Slice takes the start and end indices and returns an array that has only the elements btn them.
 * The start index is inclusive, the end index is exclusive.
 * When the end index is not given, slice will take all the elements after the start index.
 */
