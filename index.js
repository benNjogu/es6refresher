let sequence = [1, 2, 3, 4];
sequence.push(5);
sequence.push(6); //adds an element at the end of an array

console.log(sequence); //-> [ 1, 2, 3, 4, 5, 6 ]
console.log(sequence.pop()); //-> 6 //removes the last element of an array and returns it
